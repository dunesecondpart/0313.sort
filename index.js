listOfCamels = [ 8, 2, 4, 1, 3];

// sort that list of numbers
// 1, 2, 3, 4, 8
function SwapValues(a, b) {
    if (a > b) {
        let tmp = a;
        b = a;
        a = tmp;
    }
}

function sortMe(myList) {
    typeof(myList)
    // while there is a swap of number, keep looping
    isSorted = true;
    isSwapDone = true;
    indexCounter = 0;
    while (isSwapDone) {

    }

}